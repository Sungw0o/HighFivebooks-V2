# API adapter: "mock" | "real"
VITE_API_ADAPTER=mock
# Ingress single entry (real adapter)
VITE_API_BASE_URL=http://localhost:8080
# Toss Payments client key. Leave empty to use the demo pseudo payment fallback.
VITE_TOSS_CLIENT_KEY=
